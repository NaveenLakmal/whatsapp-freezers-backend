# compiled output
/dist
/node_modules
/build

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# temp directory
.temp
.tmp

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# =============================================================================
# WhatsApp Backend — Project-specific ignores
# =============================================================================

# Baileys auth state — contains WhatsApp session credentials (NEVER commit)
/auth_state/

# Downloaded media files from WhatsApp
/uploads/

# SQLite database files
*.db
*.db-shm
*.db-wal

# Keep .env tracked as a template (values are placeholders)
# Uncomment the line below if you want to exclude it entirely:
# .env

# Prisma generated client (regenerated via postinstall)
# node_modules/@prisma/client is already covered by /node_modules above
